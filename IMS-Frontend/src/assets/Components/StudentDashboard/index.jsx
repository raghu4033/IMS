import { StudentMenu } from "../../../Utils/Constants/StudentMenu";
import { Layout } from "../Common/Layout";

export const StudentDashboard = () => {
  return <Layout menu={StudentMenu}></Layout>;
};
